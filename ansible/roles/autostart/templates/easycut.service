[Unit]
Description=run easycut service and redirect stderr and stdout to a log file
After=multi-user.target

[Service]
Type=idle
ExecStart=/home/{{ pi_user }}/starteasycut.sh
User={{ pi_user }}

[Install]
WantedBy=multi-user.target
